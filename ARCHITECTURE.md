# 📊 Supabase Integration - Visual Summary

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React/Next.js)                 │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              src/app/page.tsx                            │  │
│  │  (Main map interface with pin management)               │  │
│  │                                                          │  │
│  │  State:                                                  │  │
│  │  • pins: Pin[]                                           │  │
│  │  • isCreatingPin: boolean                               │  │
│  │  • isUserTracker: boolean                               │  │
│  │  • userOrgMemberId: string | null                       │  │
│  │                                                          │  │
│  │  Effects:                                                │  │
│  │  • Load pins on mount                                   │  │
│  │  • Check user tracker status                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            ▲                                     │
│                            │ Uses                                │
│                            ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              src/services/pins.ts                        │  │
│  │  (Database operations service layer)                    │  │
│  │                                                          │  │
│  │  • createPin()                                           │  │
│  │  • fetchPins()                                           │  │
│  │  • updatePinStatus()                                    │  │
│  │  • isUserActiveTracker()                                │  │
│  │  • getUserOrgMember()                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                            ▲                                     │
│                            │ Uses                                │
│                            ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           src/lib/supabase.ts                            │  │
│  │      (Supabase client initialization)                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                ▲
                                │ Network
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SUPABASE (Backend)                          │
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────┐  │
│  │  Database        │  │  Storage         │  │  Auth       │  │
│  │                  │  │                  │  │             │  │
│  │  • pins          │  │  • pin-images    │  │ (via users) │  │
│  │  • org-member    │  │  (bucket)        │  │             │  │
│  │  • users         │  │                  │  │             │  │
│  │  • organizations │  │                  │  │             │  │
│  └──────────────────┘  └──────────────────┘  └─────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Creating a Pin

```
User fills form
       ↓
[Click Submit] handleCreatePin()
       ↓
createPin() service
       ├─→ [Optional] Upload image to storage
       │          ↓
       │    Get public image URL
       ├─→ Check if user is tracker
       │    (isUserActiveTracker)
       │          ↓
       │    Determine status
       │    (pending/confirmed)
       ├─→ Insert into 'pins' table
       │          ↓
       │    Get UUID & timestamp
       ├─→ Return new Pin object
       ↓
setPins() - Update local state
       ↓
Show success toast
       ↓
Map flies to new pin
       ↓
Update UI with new pin
```

### Confirming a Pin

```
Tracker clicks Confirm
       ↓
handleConfirmPin()
       ↓
updatePinStatus()
       ├─→ Update status to 'confirmed'
       ├─→ Set confirmed_by = org-member ID
       ├─→ Set confirmed_at = now()
       ├─→ Update database
       ↓
setPins() - Update local state
       ↓
Show success toast
       ↓
UI updates immediately
```

### Loading Pins

```
Component mounts
       ↓
useEffect runs
       ├─→ Call fetchPins()
       │    ├─→ Query pins table
       │    ├─→ JOIN with users table
       │    ├─→ Return array with creator names
       │    └─→ Convert types
       │          (damage→damaged, shelter→safe)
       │
       ├─→ Call isUserActiveTracker()
       │    ├─→ Query org-member table
       │    └─→ Check status='active'
       │
       ├─→ Call getUserOrgMember()
       │    └─→ Get org-member ID
       ↓
setPins() & setIsUserTracker()
       ↓
UI renders with loaded data
       ↓
Map shows all pins
```

---

## 📱 UI Component Structure

```
┌─────────────────────────────────────────────────────┐
│                    HomePage                         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────────────────┐  ┌────────────────┐  │
│  │    Map Container         │  │   Side Panel   │  │
│  │  - Mapbox GL Instance    │  │  ┌──────────┐  │  │
│  │  - Pin Markers           │  │  │Stats Box │  │  │
│  │  - User Location Marker  │  │  └──────────┘  │  │
│  │  - Temp Location Marker  │  │  ┌──────────┐  │  │
│  │  - Location Selection    │  │  │Recent    │  │  │
│  │  - Legend                │  │  │Reports   │  │  │
│  │                          │  │  │ ·Confirm │  │  │
│  │  [Current Location] [Add]│  │  │  button  │  │  │
│  └──────────────────────────┘  │  │          │  │  │
│                                 │  └──────────┘  │  │
│                                 │  ┌──────────┐  │  │
│                                 │  │Emergency │  │  │
│                                 │  │Kit Alert │  │  │
│                                 │  └──────────┘  │  │
│                                 └────────────────┘  │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │  Add Pin Dialog (Modal)                     │   │
│  │  ┌─────────────────────────────────────┐    │   │
│  │  │ Pin Type Toggle [Damaged] [Safe]    │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ Phone Input                         │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ Description Textarea                │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ Image Upload                        │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ [Select on Map]                     │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ [Submit Loading...] [Cancel]        │    │   │
│  │  └─────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │  Pin Detail Dialog (Modal)                  │   │
│  │  ┌─────────────────────────────────────┐    │   │
│  │  │ Phone Number                        │    │   │
│  │  │ [Status Badge: Pending/Confirmed]   │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ Description text                    │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ [Image if exists]                   │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ Creator: Name                       │    │   │
│  │  │ Time: Timestamp                     │    │   │
│  │  ├─────────────────────────────────────┤    │   │
│  │  │ [Confirm] [Deny]  (if tracker)      │    │   │
│  │  │ [Mark Delivered]  (if volunteer)    │    │   │
│  │  └─────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 🔐 Permission Matrix

```
┌─────────────────┬──────────┬──────────┬─────────────────────┐
│   User Type     │ Create   │ Confirm  │  Mark Completed     │
├─────────────────┼──────────┼──────────┼─────────────────────┤
│ Anonymous       │ Yes      │ No       │  No                 │
│ (no user_id)    │ Pending  │          │                     │
├─────────────────┼──────────┼──────────┼─────────────────────┤
│ Regular User    │ Yes      │ No       │  No                 │
│ (user_id, not   │ Pending  │          │                     │
│  in org-member) │          │          │                     │
├─────────────────┼──────────┼──────────┼─────────────────────┤
│ Tracker         │ Yes      │ Yes      │  No                 │
│ (active in      │ Auto-    │ (Pending │                     │
│  org-member)    │ confirm  │  only)   │                     │
├─────────────────┼──────────┼──────────┼─────────────────────┤
│ Supply Volunteer│ Yes      │ No       │  Yes                │
│ (supply_        │ Pending  │          │  (Confirmed         │
│  volunteer role)│          │          │   damaged only)     │
└─────────────────┴──────────┴──────────┴─────────────────────┘
```

---

## 🗄️ Database Schema Relationships

```
┌───────────────────────────────────────────────────────────┐
│                                                           │
│  ┌──────────────────────────────────────────────────┐    │
│  │           USERS TABLE                           │    │
│  │  ┌──────────────────────────────────────────┐   │    │
│  │  │ id (UUID) - PRIMARY KEY                  │   │    │
│  │  │ name (TEXT)                              │   │    │
│  │  │ email (TEXT)                             │   │    │
│  │  │ phone (TEXT)                             │   │    │
│  │  │ password (TEXT - hashed)                 │   │    │
│  │  │ created_at (TIMESTAMP)                   │   │    │
│  │  │ is_admin (BOOLEAN)                       │   │    │
│  │  └──────────────────────────────────────────┘   │    │
│  └──────────┬───────────────────────────────────────┘    │
│             │                                            │
│             │ user_id (FK) │                              
│             ▼              │                              
│  ┌─────────────────────┐   │                              
│  │ ORG-MEMBER TABLE    │   │                              
│  │ ┌─────────────────┐ │   │                              
│  │ │id (UUID) - PK   │ │   │                              
│  │ │user_id (FK) ────┼─┘   │                              
│  │ │org_id (FK)      │     │                              
│  │ │status (TEXT)    │     │                              
│  │ │type (TEXT)      │     │                              
│  │ │created_at       │     │                              
│  │ └─────────────────┘     │                              
│  └─────────────────────────┘                              
│             ▲                                             │
│             │ user_id (FK)                               │
│             │                                             │
│  ┌──────────┴──────────────────────────────────────┐    │
│  │         PINS TABLE                             │    │
│  │  ┌──────────────────────────────────────────┐  │    │
│  │  │ id (UUID) - PRIMARY KEY                  │  │    │
│  │  │ user_id (FK) → users.id                  │  │    │
│  │  │ latitude (NUMERIC)                       │  │    │
│  │  │ longitude (NUMERIC)                      │  │    │
│  │  │ type (TEXT: 'damage' | 'shelter')        │  │    │
│  │  │ phone (TEXT)                             │  │    │
│  │  │ description (TEXT)                       │  │    │
│  │  │ status (TEXT: pending/confirmed/done)    │  │    │
│  │  │ image_url (TEXT) → storage               │  │    │
│  │  │ confirmed_by (FK) → org-member.id        │  │    │
│  │  │ confirmed_at (TIMESTAMP)                 │  │    │
│  │  │ created_at (TIMESTAMP)                   │  │    │
│  │  └──────────────────────────────────────────┘  │    │
│  └───────────────────────────────────────────────────┘    │
│                                                           │
│  ┌───────────────────────────────────────────────────┐   │
│  │      STORAGE (pin-images bucket)                 │   │
│  │  ├─ pins/timestamp_filename.jpg                  │   │
│  │  ├─ pins/timestamp_filename.png                  │   │
│  │  └─ ... (public URLs stored in image_url)       │   │
│  └───────────────────────────────────────────────────┘   │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

---

## 🎯 Status Flow States

```
                    CREATE PIN
                        │
          ┌─────────────┴─────────────┐
          │                           │
    [Anonymous/User]         [Tracker User]
    (not in org-member)       (active in org-member)
          │                           │
          ▼                           ▼
    PENDING                    CONFIRMED
    (Auto-set by DB)          (Auto-set by DB)
          │                           │
          │    [Tracker confirms]     │
          │    (if pending)           │
          └──────────────┬────────────┘
                         │
                         ▼
                    CONFIRMED
                    (Manually set by tracker)
                         │
           [Supply Volunteer marks delivered]
                         │
                         ▼
                    COMPLETED
                    (Final state)
```

---

## 📈 Data Processing Pipeline

```
Input Data                Processing              Output
───────────────────────────────────────────────────────────

User submits form
  • type: "damaged"     ──→  Convert type      ──→  type: "damage"
  • phone: "123..."     ──→  Validate          ──→  Stored as-is
  • description: "..."  ──→  Store text        ──→  Stored as-is
  • location: {lat/lng} ──→  Validate coords   ──→  latitude/longitude
  • image: File         ──→  Upload to storage ──→  image_url: "https://..."
  • user_id: "uuid"     ──→  Check tracker     ──→  Status determined

                         ├─ Not a tracker → pending
                         └─ Is tracker → confirmed

  created_at: new Date() ──→  Convert to ISO   ──→  created_at: "2025-11-11T..."

Database stores          Query result          UI displays
  • UUID generated       ──→ Fetch with JOIN   ──→  Creator name from JOIN
  • Timestamp set        ──→  Convert types    ──→  Display "damaged" not "damage"
  • Status set           ──→  Format dates     ──→  Local time format
```

---

## 🔧 Configuration Checklist

```
┌─────────────────────────────────────────────────┐
│  SETUP REQUIREMENTS                             │
├─────────────────────────────────────────────────┤
│                                                 │
│  ✅ Supabase Account                            │
│     └─ Project created                          │
│                                                 │
│  ✅ Database Tables                             │
│     ├─ CREATE TABLE users                       │
│     ├─ CREATE TABLE organizations               │
│     ├─ CREATE TABLE org-member                  │
│     └─ CREATE TABLE pins                        │
│                                                 │
│  ✅ Storage Setup                               │
│     └─ Create bucket: pin-images                │
│                                                 │
│  ✅ RLS Policies                                │
│     ├─ Allow SELECT on pins                     │
│     ├─ Allow INSERT on pins                     │
│     ├─ Allow UPDATE on pins                     │
│     ├─ Allow SELECT on org-member              │
│     └─ Allow SELECT on users                    │
│                                                 │
│  ✅ Environment Variables                       │
│     ├─ NEXT_PUBLIC_SUPABASE_URL                │
│     └─ NEXT_PUBLIC_SUPABASE_ANON_KEY           │
│                                                 │
│  ✅ Node Modules                                │
│     └─ npm install @supabase/supabase-js       │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 📊 API Response Patterns

```
Success Response:
{
  success: true,
  pin: {
    id: "uuid",
    type: "damaged",
    status: "confirmed",
    phone: "123...",
    ...
  }
}

Error Response:
{
  success: false,
  error: "Error message describing what went wrong"
}

Fetch Response:
{
  success: true,
  pins: [
    { id: "uuid1", ... },
    { id: "uuid2", ... },
    ...
  ]
}
```

---

**Last Updated:** November 11, 2025  
**Version:** 1.0  
**Status:** ✅ Complete
